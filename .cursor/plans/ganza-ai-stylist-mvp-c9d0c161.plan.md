<!-- c9d0c161-c416-44b7-b5bf-f1052e60bed5 afc4ad72-0c9e-41a5-ba68-64d0cbb0b131 -->
# Разделение лендинга и квиза

## Задачи

### 1. Создать `app/[locale]/app/page.tsx`

- Перенести весь код квиза из текущего `app/[locale]/page.tsx`
- Включить все состояния: `upload` (квиз), `loader`, `result`
- Включить функции: `handleQuizComplete`, `handleRetry`, `handleViewSeason`
- Инициализировать Telegram WebApp (expand, ready)
- Использовать `router` из `@/i18n/routing` для правильной работы с локалями
- Без проверки авторизации Telegram (доступ открыт для всех)

### 2. Создать лендинг в `app/[locale]/page.tsx`

**Структура лендинга:**

**Верхняя часть:**

- LanguageSwitcher (переключатель языков)
- Бренд "GANZA" (логотип)

**Hero секция:**

- Изображение девушки перед зеркалом (`/foto/landing-girl-mirror.jpg`)
- Заголовок: "Система 4U" (градиентный стиль)
- Подзаголовок: "Персональный AI-стилист подберет идеальные луки специально для тебя"
- Кнопка CTA: "Подобрать луки" → переход на `/app`

**Секция преимуществ (3 карточки):**

- Карточка 1: ✨ "Персональный подбор" - "AI анализирует твой стиль и подбирает образы"
- Карточка 2: 🎯 "Готовые решения" - "Получи готовые луки, которые точно подойдут"
- Карточка 3: 👗 "Твой стиль" - "Уникальные образы, созданные специально для тебя"

**Дизайн:**

- Темная тема с градиентным фоном (как сейчас)
- Стеклянные карточки (glassmorphism)
- Градиентные акценты (розовый-фиолетовый)
- Адаптивная верстка для мобильных устройств

### 3. Обновить кнопку на лендинге

- Кнопка "Подобрать луки" использует `router.push('/app')` вместо `setScreen('upload')`
- Использовать `router` из `@/i18n/routing` для правильной работы с локалями

## Файлы для изменения

- `app/[locale]/page.tsx` - переписать, создать чистый лендинг
- `app/[locale]/app/page.tsx` - создать новый файл с полным кодом квиза
- `components/InstagramMockup.tsx` - создать компонент мокапа телефона с Instagram постом
- `app/globals.css` - добавить стили для Instagram мокапа

## Структура роутинга

- `/` → лендинг (русский, дефолтный)
- `/kk` → лендинг (казахский) 
- `/en` → лендинг (английский)
- `/pt-BR` → лендинг (португальский)
- `/app` → квиз (русский)
- `/kk/app` → квиз (казахский)
- `/en/app` → квиз (английский)
- `/pt-BR/app` → квиз (португальский)

### To-dos

- [ ] Создать app/[locale]/app/page.tsx с полным кодом квиза (состояния upload, loader, result)Добавить проверку авторизации Telegram в app/[locale]/app/page.tsx с редиректом на Упростить app/[locale]/page.tsx - оставить только лендинг, убрать дублирование кода Обновить кнопку 'Подобрать луки' на лендинге для перехода на /app с проверкой Telegram
- [ ] Создать лендинг