<!-- 5ce05cfc-ccd9-43ee-b391-9131b67ab5cb efc1f9ee-6c18-4576-9978-37aee5571ba4 -->
# План: Добавление селектора стилей на страницу магазина

## Цель

Создать компонент `StyleSelector`, который визуально показывает доступные и заблокированные стили одежды. Streetwear доступен для просмотра, остальные стили (Casual, Classic, Sport, Elegant) отображаются как заблокированные карточки с индикацией "скоро".

## Текущее состояние

- Страница `/shop` уже реализована
- Компонент `ArchetypeExplanation` показывает заголовок "Подборка в стиле Streetwear (доступна сейчас)"
- Все основные компоненты (ProductCard, ProductGrid, DeliveryInfo, BrandSearchAnimation) работают
- Стили в `globals.css` используют glassmorphism и градиенты

## Задача

### 1. Создание компонента StyleSelector

**Файл:** `funel_new/components/shop/StyleSelector.tsx`

**Функциональность:**

- Отображает карточки стилей в горизонтальной сетке
- Streetwear - активная карточка (яркая, с градиентом, кликабельна)
- Casual, Classic, Sport, Elegant - заблокированные карточки (затемненные, с иконкой замка, подпись "Скоро")
- Каждая карточка содержит:
  - Название стиля
  - Иконку (для заблокированных - замок)
  - Статус (доступно/скоро)
  - Визуальную индикацию доступности

**Интерфейс:**

```typescript
interface StyleSelectorProps {
  currentStyle?: 'streetwear' // текущий выбранный стиль
  onStyleSelect?: (style: string) => void // callback при клике (опционально)
}
```

**Стили:**

- Активная карточка: яркий градиент, полная непрозрачность, hover эффекты
- Заблокированная карточка: opacity 0.4-0.5, затемненный фон, иконка замка, курсор not-allowed
- Адаптивная сетка: на мобильных - 2 колонки, на десктопе - 5 колонок

### 2. Интеграция в страницу Shop

**Файл:** `funel_new/app/shop/page.tsx`

**Изменения:**

- Импортировать `StyleSelector`
- Разместить компонент после `ArchetypeExplanation` и перед `DeliveryInfo`
- Передать текущий стиль как 'streetwear'

### 3. Стили для StyleSelector

**Файл:** `funel_new/app/globals.css`

**Добавить стили:**

- `.style-selector` - контейнер селектора
- `.style-selector-title` - заголовок секции (опционально)
- `.style-cards-grid` - сетка карточек стилей
- `.style-card` - базовая карточка стиля
- `.style-card.active` - активная карточка (Streetwear)
- `.style-card.locked` - заблокированная карточка
- `.style-card-icon` - иконка стиля/замка
- `.style-card-name` - название стиля
- `.style-card-status` - статус (доступно/скоро)
- Анимации для hover и transitions

**Дизайн:**

- Использовать существующую дизайн-систему (glassmorphism, градиенты)
- Активная карточка: градиентный фон `var(--accent-gradient)`, белый текст
- Заблокированная: темный фон с низкой прозрачностью, серый текст
- Иконка замка: SVG или emoji (🔒)
- Плавные transitions при hover

## Структура компонента

```
StyleSelector
├── Заголовок (опционально): "Выберите стиль"
├── Сетка карточек
│   ├── Streetwear (активная)
│   │   ├── Иконка стиля
│   │   ├── Название "Streetwear"
│   │   └── Статус "Доступно"
│   ├── Casual (заблокированная)
│   │   ├── Иконка замка 🔒
│   │   ├── Название "Casual"
│   │   └── Статус "Скоро"
│   ├── Classic (заблокированная)
│   ├── Sport (заблокированная)
│   └── Elegant (заблокированная)
```

## Технические детали

- Компонент должен быть клиентским (`'use client'`)
- Использовать существующие CSS переменные из `:root`
- Адаптивная верстка для мобильных устройств
- Плавные анимации и transitions
- Соответствие общему стилю приложения

## Файлы для создания/изменения

**Новые файлы:**

- `funel_new/components/shop/StyleSelector.tsx`

**Изменяемые файлы:**

- `funel_new/app/shop/page.tsx` - добавить импорт и использование StyleSelector
- `funel_new/app/globals.css` - добавить стили для селектора стилей