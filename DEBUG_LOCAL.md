# 🐛 Локальная отладка Production Build

Этот гайд поможет вам отлаживать production build локально, не загружая на Vercel каждый раз.

## 🚀 Быстрый старт

### 1. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта (скопируйте из `env.example`):

```powershell
# Windows PowerShell
Copy-Item env.example .env.local

# Linux/Mac
cp env.example .env.local
```

**Или создайте вручную** файл `.env.local` и скопируйте содержимое из `env.example`.

Заполните реальными значениями переменных окружения.

### 2. Запуск production build локально

#### Вариант 1: Одной командой (рекомендуется)
```powershell
npm run debug:prod
```

Эта команда:
1. Соберет production версию (`npm run build:local`)
2. Запустит production сервер (`npm run start:local`)

#### Вариант 2: Пошагово
```powershell
# Шаг 1: Собрать production build
npm run build:local

# Шаг 2: Запустить production сервер
npm run start:local
```

### 3. Доступ к приложению

После запуска приложение будет доступно по адресу:
- **http://localhost:3000**

## 🔍 Что вы увидите при ошибках

### В Development режиме (`npm run dev`)
- Детальные ошибки в браузере
- Stack traces
- Hot reload

### В Production режиме (`npm run debug:prod`)
- Ошибки в консоли сервера
- Детальные ошибки API (благодаря нашим улучшениям)
- Реальное поведение как на Vercel

## 📝 Полезные команды

```powershell
# Development режим (с hot reload)
npm run dev

# Production build + запуск (для тестирования как на Vercel)
npm run debug:prod

# Только сборка
npm run build:local

# Только запуск production сервера
npm run start:local

# Проверка кода
npm run lint
```

## 🐛 Отладка ошибок

### Ошибки API (500, 404, etc.)

1. **Проверьте консоль сервера** - там будут детальные логи
2. **Проверьте Network tab в браузере** - увидите полный ответ с ошибкой
3. **В development режиме** - ошибки показываются в ответе API

### Ошибки сборки

```powershell
# Очистить кеш и пересобрать
Remove-Item -Recurse -Force .next
npm run build:local
```

### Ошибки локалей (404)

- Убедитесь, что middleware правильно настроен
- Проверьте, что все страницы находятся в `app/[locale]/`
- Проверьте конфигурацию в `i18n/routing.ts`

## 🔄 Workflow отладки

1. **Разработка**: Используйте `npm run dev` для быстрой разработки
2. **Тестирование**: Используйте `npm run debug:prod` перед деплоем
3. **Деплой**: Только после успешной локальной проверки

## ⚠️ Важные замечания

- Production build может отличаться от development
- Некоторые ошибки видны только в production режиме
- Всегда тестируйте production build перед деплоем на Vercel
- Переменные окружения из `.env.local` используются только локально

## 🆘 Решение проблем

### Порт 3000 занят
```powershell
# Используйте другой порт
$env:PORT=3001; npm run start:local
```

### Ошибки с переменными окружения
- Убедитесь, что `.env.local` существует
- Проверьте синтаксис (нет пробелов вокруг `=`)
- Перезапустите сервер после изменения `.env.local`

### Ошибки сборки
- Очистите `.next` папку
- Удалите `node_modules` и переустановите: `npm install`
- Проверьте версии Node.js (нужна 18+)

