# Инструкция по деплою в Vercel и обновлению в WSL

## ✅ Что было исправлено

1. Убрана обертка `.screen` из структуры лендинга (она центрировала контент вертикально)
2. Улучшены отступы и структура лендинга
3. Все файлы созданы и работают

## 📋 Шаг 1: Обновить файлы в WSL

Выполните в WSL:

```bash
cd ~/projects/funel_new

# Скопируйте исправленные файлы из Windows
cp /mnt/c/Users/rapae/OneDrive/Рабочий\ стол/funel_ganza/funel_new/app/page.tsx app/page.tsx
cp /mnt/c/Users/rapae/OneDrive/Рабочий\ стол/funel_ganza/funel_new/app/globals.css app/globals.css

# Проверьте, что файлы обновились
head -20 app/page.tsx
```

## 🧪 Шаг 2: Проверить локально

```bash
# Очистите кэш
rm -rf .next

# Запустите dev сервер
vercel dev
# или
npm run dev
```

Откройте `http://localhost:XXXXX/` и проверьте, что верстка исправлена.

## 🚀 Шаг 3: Деплой в Vercel

### Вариант 1: Через Git (рекомендуется)

```bash
cd ~/projects/funel_new

# Проверьте статус
git status

# Добавьте все изменения
git add .

# Создайте коммит
git commit -m "Fix landing page layout and add translations system"

# Отправьте в репозиторий
git push origin main
# или
git push origin master
```

Vercel автоматически задеплоит изменения после push.

### Вариант 2: Через Vercel CLI

```bash
cd ~/projects/funel_new

# Деплой в production
vercel --prod

# Или деплой в preview
vercel
```

## 🔍 Шаг 4: Проверить деплой

1. Откройте Vercel Dashboard: https://vercel.com/dashboard
2. Найдите проект `funel-ganza`
3. Проверьте, что деплой прошел успешно
4. Откройте production URL и проверьте лендинг

## ⚙️ Шаг 5: Настроить переменные окружения (если нужно)

В Vercel Dashboard → Settings → Environment Variables добавьте:

- `NEXT_PUBLIC_FB_PIXEL_ID` = `989549929881045` (если отличается)
- `N8N_CAPI_WEBHOOK_URL` = ваш webhook URL (если используется)

## 📝 Важные замечания

1. **Файлы синхронизируются автоматически** между Windows и WSL, если вы работаете в `/mnt/c/...`
2. **Если работаете в `~/projects/funel_new`** - это отдельная директория, нужно копировать файлы вручную
3. **После изменений** всегда очищайте `.next` перед запуском: `rm -rf .next`
4. **Проверяйте локально** перед деплоем в production

## 🐛 Если что-то не работает

1. Проверьте, что все файлы на месте:
   ```bash
   ls -la lib/ types/ messages/ components/
   ```

2. Проверьте, что нет ошибок компиляции:
   ```bash
   npm run build
   ```

3. Проверьте логи в Vercel Dashboard → Deployments → View Function Logs

## ✅ Чек-лист перед деплоем

- [ ] Все файлы скопированы в WSL
- [ ] Локально все работает (`vercel dev`)
- [ ] Нет ошибок компиляции (`npm run build`)
- [ ] Изменения закоммичены в Git
- [ ] Push выполнен успешно
- [ ] Деплой в Vercel прошел успешно
- [ ] Production сайт работает корректно

---

**Дата создания:** 2025-11-22
**Последнее обновление:** 2025-11-22

