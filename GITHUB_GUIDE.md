# 📤 Инструкция по загрузке проекта на GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Откройте [github.com](https://github.com)
2. Войдите в свой аккаунт
3. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
4. Заполните:
   - **Repository name**: `funel_ganza` (или любое другое имя)
   - **Description**: "FlowerFire - Telegram Mini App для определения цветотипа"
   - **Public** или **Private** (на ваш выбор)
   - ❌ **НЕ** ставьте галочки на "Add README" и "Add .gitignore" (мы уже создали эти файлы)
5. Нажмите **"Create repository"**

## Шаг 2: Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет вам команды. Используйте эти команды:

### Вариант A: Если у вас HTTPS
```bash
git remote add origin https://github.com/ваш-username/funel_ganza.git
git branch -M main
git push -u origin main
```

### Вариант B: Если у вас SSH
```bash
git remote add origin git@github.com:ваш-username/funel_ganza.git
git branch -M main
git push -u origin main
```

## Шаг 3: Выполните команды

Откройте терминал в папке проекта и выполните:

```bash
# 1. Добавьте remote (замените на ваш URL)
git remote add origin https://github.com/ваш-username/funel_ganza.git

# 2. Переименуйте ветку в main
git branch -M main

# 3. Загрузите на GitHub
git push -u origin main
```

## ✅ Готово!

Теперь ваш проект на GitHub! Откройте:
```
https://github.com/ваш-username/funel_ganza
```

## 🔄 Обновление проекта в будущем

Когда вы вносите изменения:

```bash
# 1. Добавьте изменения
git add .

# 2. Создайте коммит
git commit -m "Описание изменений"

# 3. Загрузите на GitHub
git push
```

## 🌐 Развертывание на GitHub Pages

Чтобы ваше приложение было доступно онлайн:

1. Перейдите в настройки репозитория: **Settings** → **Pages**
2. В разделе **Source** выберите:
   - Branch: `main`
   - Folder: `/mini_app` (или `/` если проект в корне)
3. Нажмите **Save**
4. Через несколько минут ваше приложение будет доступно по адресу:
   ```
   https://ваш-username.github.io/funel_ganza/
   ```

## 🚨 Важно!

Перед загрузкой убедитесь, что:
- ❌ Нет секретных ключей и паролей в коде
- ❌ Нет личных данных
- ✅ Все пути к файлам относительные (не абсолютные)
- ✅ Файл `.gitignore` настроен правильно

## 💡 Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть историю коммитов
git log --oneline

# Отменить последний коммит (если еще не запушили)
git reset --soft HEAD~1

# Посмотреть изменения
git diff
```
