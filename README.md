# GANZA AI Stylist - Telegram Mini App

Персональный AI-стилист в Telegram Mini App для определения цветотипа и подбора одежды.

## 🎨 Возможности

- Анализ цветотипа по фотографии через AI
- Рекомендации по цветам для каждого сезона (Зима, Весна, Лето, Осень)
- Подбор худи под ваш цветотип
- Интеграция с n8n для обработки фото
- Facebook Pixel для аналитики и отслеживания конверсий

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env.local` на основе `.env.local.example`:

```bash
cp .env.local.example .env.local
```

Заполните переменные:
- `N8N_WEBHOOK_URL` - URL вебхука n8n для отправки фото на анализ. По умолчанию: https://n8n-biqby-u59940.vm.elestio.app/webhook/upload-image
- `N8N_CAPI_WEBHOOK_URL` - URL вебхука n8n для отправки событий аналитики (Facebook Conversion API). По умолчанию: https://n8n-biqby-u59940.vm.elestio.app/webhook/capi-events
- `NEXT_PUBLIC_FB_PIXEL_ID` - ID Facebook Pixel (989549929881045)
- `FB_ACCESS_TOKEN` - Access Token для Facebook Conversion API (опционально)

### 3. Запуск в режиме разработки

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### 4. Сборка для продакшена

```bash
npm run build
npm start
```

## 📁 Структура проекта

```
├── app/
│   ├── api/
│   │   └── analyze/
│   │       └── route.ts          # API endpoint для анализа фото
│   ├── season/
│   │   └── [seasonName]/
│   │       └── page.tsx          # Страницы сезонов
│   ├── layout.tsx                # Корневой layout
│   ├── page.tsx                  # Главная страница
│   └── globals.css               # Глобальные стили
├── components/
│   ├── FacebookPixel.tsx         # Компонент Facebook Pixel
│   ├── PhotoUpload.tsx           # Компонент загрузки фото
│   ├── Loader.tsx                # Компонент загрузки
│   ├── ColorPalette.tsx          # Компонент палитры цветов
│   └── SeasonSlideshow.tsx       # Компонент слайдшоу для сезонов
├── public/
│   └── фото/                     # Изображения (худи, примеры цветотипов)
└── package.json
```

## 🔧 Настройка

### n8n Webhook

1. Создайте workflow в n8n с Webhook trigger
2. Скопируйте URL вебхука
3. Добавьте в `.env.local` как `N8N_WEBHOOK_URL`

Webhook должен принимать JSON с полями:
```json
{
  "image": "base64_encoded_image",
  "user_id": "telegram_user_id",
  "first_name": "user_name",
  "timestamp": "ISO_timestamp"
}
```

И возвращать результат анализа:
```json
{
  "season": "Зима",
  "colors": ["#000000", "#FFFFFF", ...],
  "description": "Описание цветотипа",
  "product_image": "url_to_product",
  "product_name": "Название товара",
  "price": "2 990 ₽",
  "product_link": "url_to_product"
}
```

### Facebook Pixel

1. Создайте Pixel в Facebook Business Manager
2. Скопируйте Pixel ID
3. Добавьте в `.env.local` как `NEXT_PUBLIC_FB_PIXEL_ID`

События, которые отслеживаются:
- `PageView` - просмотр страницы
- `InitiateCheckout` - начало анализа
- `CompleteRegistration` - завершение анализа
- `Lead` - отправка фото на анализ
- `Purchase` - клик на покупку

## 📱 Интеграция с Telegram

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Зарегистрируйте Mini App:
   ```
   /newapp
   ```
3. Разместите приложение на хостинге (Vercel, Netlify, или другой)
4. Укажите URL в настройках бота

## 🌐 Деплой

### Vercel (рекомендуется)

1. Подключите репозиторий к Vercel
2. Добавьте переменные окружения в настройках проекта
3. Деплой произойдет автоматически

### Другие платформы

Проект можно задеплоить на любую платформу, поддерживающую Next.js:
- Netlify
- Railway
- Render
- DigitalOcean App Platform

## 🎨 Технологии

- **Next.js 14** - React фреймворк
- **TypeScript** - типизация
- **Telegram Web App SDK** - интеграция с Telegram
- **Facebook Pixel** - аналитика
- **n8n** - автоматизация и обработка данных

## 📄 Лицензия

MIT

## 👤 Автор

GANZA Team
