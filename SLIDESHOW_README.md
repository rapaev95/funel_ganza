# Автоматическое обновление слайдшоу

Этот скрипт автоматически сканирует папки с изображениями цветотипов и обновляет HTML файлы сезонов.

## Как использовать:

### 1. Добавьте изображения в папки:
```
фото/цветотипы/
├── winter/    (Зима)
├── spring/    (Весна)
├── summer/    (Лето)
└── autumn/    (Осень)
```

Поддерживаемые форматы: `.jpg`, `.jpeg`, `.png`, `.webp`, `.gif`

### 2. Запустите скрипт:
```bash
node update-slideshow-images.js
```

### 3. Готово!
Скрипт автоматически:
- Найдет все изображения в каждой папке
- Обновит соответствующие HTML файлы
- Покажет сколько изображений найдено для каждого сезона

## Пример вывода:
```
🔄 Сканирование папок с изображениями...

✅ Обновлен season_winter.html: 5 изображений
✅ Обновлен season_spring.html: 4 изображений
✅ Обновлен season_summer.html: 6 изображений
✅ Обновлен season_autumn.html: 3 изображений

✨ Готово! Все HTML файлы обновлены.
```

## Автоматизация:

Вы можете добавить скрипт в `package.json`:
```json
{
  "scripts": {
    "update-images": "node update-slideshow-images.js"
  }
}
```

Тогда запуск будет еще проще:
```bash
npm run update-images
```

## Важно:
- Просто добавляйте новые фото в папки цветотипов
- Запускайте скрипт после добавления новых изображений
- Слайдшоу автоматически покажет все найденные фото
