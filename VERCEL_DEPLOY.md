# 🚀 Деплой на Vercel - Пошаговая инструкция

## ✅ Чек-лист перед деплоем

- [x] TypeScript ошибки исправлены
- [x] Дублирующие файлы удалены (`app/season/`)
- [x] `.gitignore` настроен
- [x] `package.json` содержит все зависимости
- [x] Конфигурация Next.js (`next.config.js`) готова
- [x] Переменные окружения подготовлены (`.env.example`)

## 📋 Шаг 1: Загрузка на GitHub

Если еще не загружено:

```bash
git add .
git commit -m "Prepare for Vercel deployment"
git push origin main
```

## 📋 Шаг 2: Создание проекта на Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите **"Add New... → Project"**
4. Выберите репозиторий `funel_ganza`
5. Нажмите **"Import"**

## ⚙️ Шаг 3: Настройка проекта

### Build & Development Settings:

- **Framework Preset**: `Next.js` (автоматически определится)
- **Build Command**: `npm run build` (по умолчанию)
- **Output Directory**: `.next` (по умолчанию)
- **Install Command**: `npm install` (по умолчанию)
- **Root Directory**: `./` (оставить пустым)

### Environment Variables:

Добавьте следующие переменные окружения в разделе **"Environment Variables"**:

```
N8N_WEBHOOK_URL=https://n8n-biqby-u59940.vm.elestio.app/webhook/upload-image
N8N_CAPI_WEBHOOK_URL=https://n8n-biqby-u59940.vm.elestio.app/webhook/capi-events
NEXT_PUBLIC_FB_PIXEL_ID=989549929881045
NEXT_PUBLIC_SITE_URL=https://your-project-name.vercel.app
```

⚠️ **Важно**: Замените `your-project-name.vercel.app` на реальный URL вашего проекта после первого деплоя.

## 🎯 Шаг 4: Деплой

1. Нажмите **"Deploy"**
2. Vercel автоматически:
   - Склонирует репозиторий
   - Установит зависимости (`npm install`)
   - Соберет проект (`npm run build`)
   - Задеплоит на production

## 🔧 Шаг 5: После первого деплоя

1. Скопируйте URL вашего проекта (например, `vibelook.vercel.app`)
2. Обновите переменную окружения:
   - Зайдите в **Settings → Environment Variables**
   - Найдите `NEXT_PUBLIC_SITE_URL`
   - Обновите значение на реальный URL
3. Перезапустите деплой: **Deployments → ... → Redeploy**

## 📱 Шаг 6: Настройка Telegram Bot

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/setmenubutton`
3. Выберите вашего бота
4. Отправьте:
   ```
   text: Открыть приложение
   url: https://your-project-name.vercel.app
   ```
5. Отправьте команду `/newapp`
6. Выберите бота
7. Укажите URL: `https://your-project-name.vercel.app`

## 🔄 Автоматические деплои

После настройки каждый push в `main` ветку автоматически запустит новый деплой:

```bash
git add .
git commit -m "Update feature"
git push origin main
```

Vercel автоматически:
- Создаст preview deployment
- Запустит билд
- Задеплоит изменения

## 🐛 Troubleshooting

### Ошибка: "Build failed"

1. Проверьте логи в Vercel Dashboard
2. Запустите локально:
   ```bash
   npm run build
   ```
3. Исправьте ошибки и сделайте push

### Ошибка: "Module not found"

1. Убедитесь, что все зависимости в `package.json`
2. Очистите кеш Vercel:
   - Settings → General → Clear Build Cache

### Ошибка: "Environment variables not found"

1. Проверьте, что переменные добавлены в Settings → Environment Variables
2. Убедитесь, что переменные с `NEXT_PUBLIC_` доступны на клиенте
3. Redeploy проекта

## 📊 Мониторинг

После деплоя доступны:

- **Analytics**: просмотры страниц, производительность
- **Logs**: логи в реальном времени
- **Deployments**: история всех деплоев
- **Domains**: управление доменами

## 🌐 Кастомный домен (опционально)

1. Перейдите в **Settings → Domains**
2. Нажмите **"Add"**
3. Введите ваш домен
4. Следуйте инструкциям для настройки DNS

## ✅ Готово!

Ваше приложение доступно по адресу:
```
https://your-project-name.vercel.app
```

Telegram mini app доступен через:
```
https://t.me/vibelook_bot/webapp
```

