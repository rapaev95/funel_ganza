# Seller Dashboard

Кабинет селлера для управления товарами и кампаниями Vibelook.

## 📚 Документация проекта

**ВАЖНО:** При разработке новых функций и планировании задач ВСЕГДА обращайся к основной документации:

- **[VIBELOOK_SELLER.md](../seller-dashboard/docs/VIBELOOK_SELLER.md)** - полное техническое задание проекта, архитектура, все страницы, API, UI/UX спецификации
- [SUPABASE_DATABASE_SETUP.md](../seller-dashboard/docs/SUPABASE_DATABASE_SETUP.md) - настройка базы данных
- [DESIGN_AND_MANUAL_TASKS.md](../seller-dashboard/docs/DESIGN_AND_MANUAL_TASKS.md) - дизайн и ручные задачи
- [N8N_WORKFLOWS_SETUP.md](../seller-dashboard/docs/N8N_WORKFLOWS_SETUP.md) - настройка n8n workflows

Файл `.cursorrules` в этой папке автоматически указывает AI на документацию при планировании задач.

## Установка

```bash
npm install
```

## Настройка переменных окружения

### Вариант 1: Автоматическое копирование из funel_new (рекомендуется)

Если у вас уже есть настроенный `.env.development.local` в папке `funel_new`, используйте скрипт:

**Windows (PowerShell):**
```powershell
.\scripts\copy-env.ps1
```

Скрипт автоматически скопирует нужные переменные:
- `TELEGRAM_BOT_TOKEN`
- `NEXT_PUBLIC_TELEGRAM_WIDGET_BOT_USERNAME`
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `SUPABASE_URL`

**Дополнительные переменные для генерации луков:**
- `N8N_LOOKS_GENERATION_WEBHOOK_URL` - URL webhook n8n для генерации луков (опционально, для демо используется заглушка)

### Вариант 2: Ручная настройка

1. Скопируйте файл `.env.example` в `.env.local`:
   ```bash
   cp .env.example .env.local
   ```

2. Откройте `.env.local` и заполните реальными значениями:
   - `TELEGRAM_BOT_TOKEN` - получите от @BotFather
   - `NEXT_PUBLIC_TELEGRAM_WIDGET_BOT_USERNAME` - username вашего бота (без @)
   - `N8N_LOOKS_GENERATION_WEBHOOK_URL` - URL webhook n8n для генерации луков (опционально)
   - Supabase переменные (опционально, если используете Supabase)

**Важно:** Файл `.env.local` не попадает в git и содержит секретные данные.

## Запуск

```bash
npm run dev
```

Приложение будет доступно на http://localhost:3001

## Структура проекта

```
seller/
├── app/
│   ├── api/
│   │   └── auth/
│   │       ├── telegram/route.ts    # Обработка авторизации через Telegram
│   │       ├── session/route.ts     # Проверка сессии
│   │       └── logout/route.ts      # Выход из системы
│   ├── auth/
│   │   └── page.tsx                 # Страница авторизации
│   ├── dashboard/
│   │   └── page.tsx                 # Главная страница dashboard
│   ├── layout.tsx                   # Корневой layout
│   ├── page.tsx                     # Главная страница (редирект на /auth)
│   └── globals.css                  # Глобальные стили
├── components/
│   └── TelegramLogin.tsx            # Компонент авторизации через Telegram
├── lib/
│   ├── auth-context.tsx             # Контекст для управления авторизацией
│   ├── supabase.ts                  # Серверный клиент Supabase
│   └── supabase-client.ts           # Клиентский клиент Supabase
└── middleware.ts                    # Middleware для защиты роутов
```

## Авторизация

Авторизация реализована через Telegram Login Widget. Процесс:

1. Пользователь нажимает кнопку "Войти через Telegram"
2. Telegram Widget открывает окно авторизации
3. После успешной авторизации данные пользователя отправляются на `/api/auth/telegram`
4. Сервер проверяет подпись данных от Telegram
5. Пользователь сохраняется/обновляется в Supabase (если настроен)
6. Создается сессия (cookie)
7. Пользователь перенаправляется на `/dashboard`

### Настройка Telegram Login Widget

**Важно:** Перед использованием необходимо настроить бота в BotFather:

1. Создайте бота через [@BotFather](https://t.me/BotFather) (`/newbot`)
2. Привяжите домен к боту (`/setdomain`)
   - Для локальной разработки: `localhost`
   - Для продакшена: ваш домен (например: `seller.vibelook.com`)
3. Настройте переменные окружения (см. выше)

📖 **Подробная инструкция:** [TELEGRAM_LOGIN_SETUP.md](./docs/TELEGRAM_LOGIN_SETUP.md)

## Интеграция с Supabase

Приложение автоматически:
- Создает запись в таблице `sellers` при первой авторизации
- Обновляет информацию о пользователе при повторной авторизации
- Устанавливает trial период на 7 дней для новых пользователей

## Защита роутов

Middleware автоматически защищает все роуты кроме `/auth` и `/api/auth`. Неавторизованные пользователи перенаправляются на страницу авторизации.

## Дальнейшая разработка

- Добавление страниц dashboard согласно дизайну
- Реализация функционала управления товарами
- Интеграция с API Wildberries/Ozon
- Управление подписками и тарифами
